name: Publish Stable Image

on:
  push:
    tags:
      - 'v*'

jobs:
  publish:
    permissions:
      contents: read
      packages: write
    uses: ./.github/workflows/build-and-push-image.yml
    with:
      tag: ${{ github.ref_name }}
